<ul class="nav nav-pills d-flex justify-content-between mb-5">
  <li class="nav-item">
    <a class="nav-link d-flex align-items-center text-secondary gap-1" href="/user/profile"> <i class="bi bi-person-fill"></i> <span class="d-none d-md-inline"><%= profileTabs.buttons.profile %></span></a>
  </li>
  <!-- <li class="nav-item">
    <a class="nav-link d-flex align-items-center text-secondary gap-1" href="/user/settings"> <i class="bi bi-sliders"></i> <span class="d-none d-md-inline">Settings</span></a>
  </li> -->
  <li class="nav-item">
    <a class="nav-link d-flex align-items-center text-secondary gap-1" href="/user/watchlist"> <i class="bi bi-bookmark-heart-fill"></i> <span class="d-none d-md-inline"><%= profileTabs.buttons.followedPets %></span></a>
  </li>
  <li class="nav-item">
    <a class="nav-link d-flex align-items-center text-secondary gap-1" href="/user/favorites"> <i class="bi bi-bookmark-star-fill"></i> <span class="d-none d-md-inline"><%= profileTabs.buttons.followedServices %></span></a>
  </li>
  <li class="nav-item">
    <a class="nav-link d-flex align-items-center text-secondary gap-1" href="/user/pets"><i class="bi bi-heart-fill"></i> <span class="d-none d-md-inline"><%= profileTabs.buttons.myPets %></span></a>
  </li>
  <li class="nav-item">
    <a class="nav-link d-flex align-items-center text-secondary gap-1" href="/user/services"><i class="bi bi-star-fill"></i> <span class="d-none d-md-inline"><%= profileTabs.buttons.myServices %></span></a>
  </li>
</ul>

<!-- < if (typeof pets !== 'undefined' && pets.length > 0) { %>
<p><= pets.length %></p>
< } %>-->
<style>
  .active {
    background-color: #6c757d !important;
    color: #f8f9fa !important;
  }
  .nav-link.active {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: fit-content; /* Adjust the value to fit your desired width */
  }

  @media (max-width: 767px) {
    .nav-link .bi {
      font-size: 24px; /* Adjust the font size as needed */
    }
  }
</style>

<script>
  // Get the current URL path
  var path = window.location.pathname;

  // Get all the navigation links
  var navLinks = document.querySelectorAll('.nav-link');

  // Loop through each navigation link
  navLinks.forEach(function (link) {
    // Check if the link's href matches the current path
    if (link.getAttribute('href') === path) {
      // Add the active class to the matching link
      link.classList.add('active');
    }
  });
</script>
